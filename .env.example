# Clawcut Environment Configuration Example
# Copy this to .env and modify as needed

# The absolute path where rendered videos should be stored.
# If not set, Clawcut defaults to the local 'outputs/' folder.
# CLAWCUT_OUTPUT_DIR=/path/to/your/public/media/folder
